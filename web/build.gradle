apply plugin: 'war'

dependencies{
    compile(
            project(':service'),
            ["org.springframework:spring-webmvc:4.3.3.RELEASE"]
    )

    providedCompile(
            ['javax.servlet:javax.servlet-api:3.1.0']
    )

}